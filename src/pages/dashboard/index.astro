---
/**
 * Dashboard Principal - Mini-CRM SoftControl
 */
import Layout from '../../layouts/Layout.astro';
import { dashboardService } from '../../lib/services/dashboardService';
import { getCurrentUserProfile, isAdmin } from '../../lib/utils/roleHelper';

// Obtener estadísticas
let stats = {
  total_clients: 0,
  total_products: 0,
  active_licenses: 0,
  pending_payment_licenses: 0
};

// Obtener datos de ventas
let salesData = {
  labels: [] as string[],
  data: [] as number[]
};

// Obtener perfil del usuario
const accessToken = Astro.cookies.get('sb-access-token')?.value;
const userProfile = accessToken ? await getCurrentUserProfile(accessToken) : null;
const userIsAdmin = isAdmin(userProfile);

try {
  stats = await dashboardService.getStats();
  salesData = await dashboardService.getSalesData();
  console.log('Datos de ventas:', salesData);
} catch (error) {
  console.error('Error al obtener estadísticas:', error);
}
---

<Layout title="Dashboard - SoftControl CRM">
  <div class="container">
    <div class="page-header">
      <h1 class="page-title">Dashboard</h1>
    </div>

    <!-- Tarjetas de estadísticas -->
    <div class="stats-grid">
      <!-- Total Clientes -->
      <a href="/clients" class="stat-card">
        <div class="stat-icon" style="background: linear-gradient(135deg, #6366f1, #8b5cf6);">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19.128a9.38 9.38 0 002.625.372 9.337 9.337 0 004.121-.952 4.125 4.125 0 00-7.533-2.493M15 19.128v-.003c0-1.113-.285-2.16-.786-3.07M15 19.128v.106A12.318 12.318 0 018.624 21c-2.331 0-4.512-.645-6.374-1.766l-.001-.109a6.375 6.375 0 0111.964-3.07M12 6.375a3.375 3.375 0 11-6.75 0 3.375 3.375 0 016.75 0zm8.25 2.25a2.625 2.625 0 11-5.25 0 2.625 2.625 0 015.25 0z" />
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Clientes</p>
          <p class="stat-value">{stats.total_clients}</p>
        </div>
      </a>

      <!-- Total Productos -->
      <a href="/products" class="stat-card">
        <div class="stat-icon" style="background: linear-gradient(135deg, #ec4899, #f43f5e);">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M20.25 7.5l-.625 10.632a2.25 2.25 0 01-2.247 2.118H6.622a2.25 2.25 0 01-2.247-2.118L3.75 7.5M10 11.25h4M3.375 7.5h17.25c.621 0 1.125-.504 1.125-1.125v-1.5c0-.621-.504-1.125-1.125-1.125H3.375c-.621 0-1.125.504-1.125 1.125v1.5c0 .621.504 1.125 1.125 1.125z" />
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Productos</p>
          <p class="stat-value">{stats.total_products}</p>
        </div>
      </a>

      <!-- Licencias Activas -->
      <a href="/licenses?status=activa" class="stat-card">
        <div class="stat-icon" style="background: linear-gradient(135deg, #10b981, #14b8a6);">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Licencias Activas</p>
          <p class="stat-value">{stats.active_licenses}</p>
        </div>
      </a>

      <!-- Licencias Pendientes -->
      <a href="/licenses?status=pendiente_pago" class="stat-card">
        <div class="stat-icon" style="background: linear-gradient(135deg, #f59e0b, #f97316);">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Pendientes de Pago</p>
          <p class="stat-value">{stats.pending_payment_licenses}</p>
        </div>
      </a>
    </div>

    <!-- Gráfica de Ventas -->
    <div class="chart-section">
      <div class="chart-header">
        <h2>Ventas de Licencias</h2>
        <div class="chart-filters">
          <button class="filter-btn" data-interval="7d">7 días</button>
          <button class="filter-btn" data-interval="30d">30 días</button>
          <button class="filter-btn" data-interval="3m">3 meses</button>
          <button class="filter-btn active" data-interval="6m">6 meses</button>
          <button class="filter-btn" data-interval="12m">12 meses</button>
        </div>
      </div>
      <div class="chart-container">
        <canvas id="salesChart"></canvas>
      </div>
    </div>

    <!-- Accesos rápidos - Solo para Admin -->
    {userIsAdmin && (
      <div class="quick-actions">
        <h2 style="margin-bottom: 1.5rem;">Accesos Rápidos</h2>
        <div class="actions-grid">
          <a href="/clients/new" class="action-card">
            <div class="action-icon" style="background: linear-gradient(135deg, #6366f1, #8b5cf6);">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19 7.5v3m0 0v3m0-3h3m-3 0h-3m-2.25-4.125a3.375 3.375 0 11-6.75 0 3.375 3.375 0 016.75 0zM4 19.235v-.11a6.375 6.375 0 0112.75 0v.109A12.318 12.318 0 0110.374 21c-2.331 0-4.512-.645-6.374-1.766z" />
              </svg>
            </div>
            <div>
              <h3>Nuevo Cliente</h3>
              <p>Registrar un nuevo cliente</p>
            </div>
          </a>

          <a href="/products/new" class="action-card">
            <div class="action-icon" style="background: linear-gradient(135deg, #ec4899, #f43f5e);">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
              </svg>
            </div>
            <div>
              <h3>Nuevo Producto</h3>
              <p>Añadir un nuevo producto</p>
            </div>
          </a>

          <a href="/licenses/new" class="action-card">
            <div class="action-icon" style="background: linear-gradient(135deg, #10b981, #14b8a6);">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 5.25a3 3 0 013 3m3 0a6 6 0 01-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 1121.75 8.25z" />
              </svg>
            </div>
            <div>
              <h3>Nueva Licencia</h3>
              <p>Asignar una nueva licencia</p>
            </div>
          </a>
        </div>
      </div>
    )}

    <!-- Mensaje para Staff -->
    {!userIsAdmin && (
      <div class="info-card">
        <p><strong>Modo Solo Lectura</strong> - Tu rol de Staff te permite ver toda la información pero no realizar cambios.</p>
      </div>
    )}
  </div>
</Layout>

<!-- Cargar Chart.js desde CDN -->
<script is:inline src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>

<script is:inline define:vars={{ salesData }}>
  let chartInstance = null;
  
  // Función para inicializar la gráfica
  function initChart(data) {
    console.log('Iniciando gráfica con datos:', data);
    
    const ctx = document.getElementById('salesChart');
    
    if (!ctx) {
      console.error('No se encontró el canvas salesChart');
      return;
    }
    
    if (!data || !data.labels || data.labels.length === 0) {
      console.error('No hay datos de ventas para mostrar:', data);
      return;
    }
    
    // Esperar a que Chart.js esté disponible
    if (typeof Chart === 'undefined') {
      console.log('Chart.js no está cargado todavía, esperando...');
      setTimeout(() => initChart(data), 100);
      return;
    }
    
    console.log('Creando gráfica con Chart.js...');
    
    // Destruir la instancia anterior si existe
    if (chartInstance) {
      chartInstance.destroy();
    }
    
    chartInstance = new Chart(ctx, {
      type: 'line',
      data: {
        labels: data.labels,
        datasets: [{
          label: 'Ventas (€)',
          data: data.data,
          borderColor: '#6366f1',
          backgroundColor: 'rgba(99, 102, 241, 0.1)',
          borderWidth: 3,
          fill: true,
          tension: 0.4,
          pointRadius: 5,
          pointHoverRadius: 7,
          pointBackgroundColor: '#6366f1',
          pointBorderColor: '#fff',
          pointBorderWidth: 2
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            display: false
          },
          tooltip: {
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            padding: 12,
            titleFont: {
              size: 14
            },
            bodyFont: {
              size: 13
            },
            callbacks: {
              label: function(context) {
                return 'Ventas: ' + context.parsed.y.toFixed(2) + ' €';
              }
            }
          }
        },
        scales: {
          y: {
            beginAtZero: true,
            ticks: {
              callback: function(value) {
                return value + ' €';
              }
            },
            grid: {
              color: 'rgba(0, 0, 0, 0.05)'
            }
          },
          x: {
            grid: {
              display: false
            }
          }
        }
      }
    });
    
    console.log('Gráfica creada exitosamente');
  }

  // Función para cargar datos con un intervalo específico
  async function loadSalesData(interval) {
    console.log('Cargando datos para intervalo:', interval);
    
    try {
      const response = await fetch(`/api/sales/data?interval=${interval}`);
      if (!response.ok) {
        throw new Error('Error al cargar datos de ventas');
      }
      
      const data = await response.json();
      console.log('Datos recibidos:', data);
      
      initChart(data);
    } catch (error) {
      console.error('Error al cargar datos de ventas:', error);
    }
  }

  // Configurar event listeners para los botones de filtro
  function setupFilters() {
    const filterButtons = document.querySelectorAll('.filter-btn');
    
    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        // Remover clase active de todos los botones
        filterButtons.forEach(btn => btn.classList.remove('active'));
        
        // Agregar clase active al botón clickeado
        button.classList.add('active');
        
        // Cargar datos con el intervalo seleccionado
        const interval = button.getAttribute('data-interval');
        loadSalesData(interval);
      });
    });
  }

  // Inicializar cuando el DOM esté listo
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      initChart(salesData);
      setupFilters();
    });
  } else {
    initChart(salesData);
    setupFilters();
  }
</script>

<style>
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .stat-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    box-shadow: var(--shadow-sm);
    transition: var(--transition);
    text-decoration: none;
    color: inherit;
    cursor: pointer;
  }

  .stat-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
    border-color: var(--color-primary);
  }

  .stat-icon {
    width: 4rem;
    height: 4rem;
    border-radius: var(--radius);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .icon {
    width: 2rem;
    height: 2rem;
    color: white;
  }

  .stat-content {
    flex: 1;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--color-text-light);
    margin-bottom: 0.25rem;
  }

  .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text);
  }

  .quick-actions {
    margin-top: 2rem;
  }

  .quick-actions h2 {
    margin-bottom: 1.5rem;
    color: var(--color-text);
  }

  .actions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .action-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 2rem;
    text-align: center;
    transition: var(--transition);
    text-decoration: none;
    color: inherit;
  }

  .action-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
    border-color: var(--color-primary);
  }

  .action-icon {
    width: 4rem;
    height: 4rem;
    margin: 0 auto 1rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    border-radius: var(--radius);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .action-icon svg {
    width: 2rem;
    height: 2rem;
    color: white;
  }

  .action-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  .action-card p {
    color: var(--color-text-light);
    font-size: 0.9375rem;
  }

  .chart-section {
    margin: 3rem 0;
  }

  .chart-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .chart-header h2 {
    margin: 0;
    color: var(--color-text);
  }

  .chart-filters {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .filter-btn {
    padding: 0.5rem 1rem;
    border: 1px solid var(--color-border);
    background: var(--color-bg);
    border-radius: var(--radius);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-text-light);
    cursor: pointer;
    transition: all 0.2s;
  }

  .filter-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .filter-btn.active {
    background: var(--color-primary);
    border-color: var(--color-primary);
    color: white;
  }

  .chart-container {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 2rem;
    box-shadow: var(--shadow-sm);
    height: 400px;
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }

    .actions-grid {
      grid-template-columns: 1fr;
    }

    .chart-container {
      padding: 1rem;
      height: 300px;
    }
  }
</style>
